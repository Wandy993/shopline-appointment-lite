# Use the one-click Mac release command delivered alongside the Appointment Lite v0.6.0.1 ZIP.
# The command performs the safe overlay, tests, Git push, and Railway deployment.
# Theme Extension push is skipped because this release contains no storefront extension changes.
