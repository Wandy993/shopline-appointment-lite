# Use the one-click Mac command delivered alongside the v0.6.0 Google Calendar Foundation release ZIP.
# The command verifies the final ZIP SHA-256 before overlaying the existing Appointment Lite Git repository.
# v0.6.0 does not change the Theme App Extension, so sl extension push is intentionally skipped.
